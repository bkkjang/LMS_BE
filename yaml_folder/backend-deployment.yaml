# 백엔드(Spring Boot, LMS_BE) - Deployment
# replicas: 2 + RollingUpdate (무중단)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: library
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          # ECR 경로는 확정. 태그(7자리 git SHA)만 C팀이 빌드하면 받아서 교체.
          image: 416225317325.dkr.ecr.ap-southeast-2.amazonaws.com/lms-be:484ed41
          ports:
            - containerPort: 8080
          # DB 접속정보(URL, USERNAME)는 ConfigMap에서 주입
          envFrom:
            - configMapRef:
                name: backend-config
          # DB 비밀번호만 Secret에서 주입 (DB와 같은 Secret 공유)
          env:
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: MYSQL_ROOT_PASSWORD
          # 오토스케일(HPA)이 CPU 기준으로 동작하려면 requests 명시 필수
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
