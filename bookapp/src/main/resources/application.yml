server:
  port: 8080

spring:
  application:
    name: bookapp
  datasource:
    url: jdbc:mysql://localhost:3306/bookdb
    username: root
    password: Wkdqhdrud2@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true