spring:
  application:
    name: bookapp

  datasource:
    url: jdbc:mysql://localhost:3306/bookdb?createDatabaseIfNotExist=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: aivle

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ohhyun1231@gmail.com
    password: yzajuhxhvvrfdphl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  cache:
    type: simple  # 인메모리 캐시 (ConcurrentHashMap) — 추가 의존성 없이 @Cacheable 동작

  sql:
    init:
      mode: never
      encoding: UTF-8

jwt:
  secret: YWl2bGVsbXNwcm9qZWN0and0c2VjcmV0a2V5MjAyNmFpdmxlbG1z
  expiration: 86400000

naver:
  client-id: HyOHzX3Vwm6lvMggJ_8k
  client-secret: JKU27BdVCV

# OpenAI 프록시용 키 — 환경변수 OPENAI_API_KEY 로 주입(없으면 빈 값).
# 절대 깃에 직접 키를 커밋하지 말 것. 새로 발급한 키를 OPENAI_API_KEY 로 넣어 실행.
openai:
  api-key: ${OPENAI_API_KEY:}

server:
  port: 8080
